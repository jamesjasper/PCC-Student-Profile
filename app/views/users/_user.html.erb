<tr class="candidates-list">
  <td class="title justify-content-center">
    <div class="candidate-list-details">
      <div class="candidate-list-info">
        <div class="candidate-list-title">
          <h5 class="mb-0"><%= link_to user.full_name, user_path(user) %></h5>
        </div>
        <div class="candidate-list-option">
          <ul class="list-unstyled">
            <li>email: <%= user.email  %></li>
            <li>username: <%= user.username %></li>
          </ul>
        </div>
      </div>
    </div>
    </td>
    <td class="align-middle">
      <ul class="mb-0 d-flex justify-content-lg-center align-content-center list-unstyled">
        <li>
          <%= link_to user_path(user),class: "btn btn-sm btn-primary m-1" do %>
            <i class="bi bi-binoculars-fill"></i>
          <% end %>
        </li>
        <% if current_user?(user) || admin? %>
        <li>
          <%= link_to edit_user_path(user), class: "btn btn-sm btn-primary m-1" do %>
            <i class="bi bi-pencil-fill"></i>
          <% end %>
        </li>
        <li>
          <%= link_to user_path(user), method: :delete, data: {confirm: 'Are you sure you want to delete this user?'},
            class: "btn btn-sm btn-danger m-1" do %>
            <i class="bi bi-trash-fill"></i>
          <% end %>
        </li>
        <% end %>
      </ul>
    </td>
</tr>